enum UserRole {
  none,
  parent,
  child,
}

enum UserGender {
  none,
  male,
  female,
}

enum KidProfileTabs {
  jars,
  notifications,
  goals,
}

enum Jars {
  spendingJar,
  savingJar,
}

enum JarState {
  nullJar,
  empty,
  filled,
}

enum AmountAdditionMode {
  jarCreation,
  requestMoney,
  addMoney,
}
enum MoneyAnimationType {
  added,
  removed,
}
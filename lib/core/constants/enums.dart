enum UserRole {
  NONE,
  PARENT,
  CHILD,
}

enum UserGender {
  None,
  Male,
  Female,
}

enum KidProfileTabs {
  Jars,
  Notifications,
  Goals,
}

enum Jars {
  spendingJar,
  savingJar,
}

enum JarState {
  null_jar,
  empty,
  filled,
}

enum AmountAdditionMode {
  jarCreation,
  requestMoney,
  addMoney,
}

import 'package:flutter/material.dart';

class KidGoals extends StatelessWidget {
  const KidGoals({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: Center(
        child: Text('KidGoals'),
      )),
    );
  }
}
